import React from "react";
import styled from "styled-components";
const ProfileImage = () => {
  return <Wrapper>ProfileImage</Wrapper>;
};

const Wrapper = styled.div``;

export default ProfileImage;
